name: counter_app
description: Counter demo app implemented with 'reduced' API and 'reduced_bloc' implementation as an example of the usage of the 'reduced_bloc' package.
publish_to: 'none' 
version: 0.6.0+8

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
#  reduced: 0.6.0
  reduced: 
    git:
      url: https://github.com/partmaster/reduced.git
      ref: '337e823'
  reduced_bloc: 
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
