name: reduced_bloc
version: 0.4.0-beta.1
description: Implementation of the state management API 'reduced' on top of the state management framework 'Bloc'.
homepage: https://github.com/partmaster/reduced_bloc
repository: https://github.com/partmaster/reduced_bloc.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
#  reduced: 0.4.0
  reduced: 
    git:
      url: https://github.com/partmaster/reduced.git
      ref: v0.4.0-beta.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  bloc_test: ^9.1.1
